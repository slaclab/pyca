Restructure pyca to simplify compilation using setup.py.

Now build with:
    python setup.py build

Install with:
    python setup.py install --prefix=DIR

Build a tarball with:
    python setup.py sdist

If you make a new tarball, be sure to edit the version in setup.py.
